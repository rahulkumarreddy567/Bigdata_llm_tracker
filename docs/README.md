# Deployment Guides

This folder contains the supported deployment guides for the project:

- `CLOUD_DEPLOY.md`: generic VM deployment with Docker Compose
- `ORACLE_FREE_TIER_DEPLOY.md`: Oracle Cloud Always Free VM setup

For local development and the main project overview, start from the repository root `README.md`.
