name: 'llm_tracker'
version: '1.0.0'
config-version: 2

profile: 'llm_tracker'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  llm_tracker:
    formatted:
      +materialized: table
      +schema: formatted
    combined:
      +materialized: table
      +schema: combined
